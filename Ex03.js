// "Escreva um programa em JavaScript que declare três constantes, 'largura', 'altura' e 'profundidade', e atribua a elas os valores 10, 5 e 3, respectivamente.
// Em seguida, o programa deve calcular o volume de uma caixa multiplicando a largura pela altura e pela profundidade, e armazenar o resultado na constante 'volume'. Por fim, o programa deve imprimir no console a frase 'O volume da caixa é: ' seguida pelo valor do volume calculado."

const largura = 10;
const altura = 5;
const profundidade = 3;

//Cáculo do volume

const volume = largura*altura*profundidade;

console.log ("O volume da caixa é: ", volume);

